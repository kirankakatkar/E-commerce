import * as React from 'react';

interface ICatalogSidebarProps {
}

const CatalogSidebar: React.FunctionComponent<ICatalogSidebarProps> = (props) => {
  return <></> ;
};

export default CatalogSidebar;
